# @package _global_

defaults:
  - _self_
  - data: bt_mri # choose datamodule with `test_dataloader()` for evaluation
  # Must match the architecture the checkpoint was trained with; override per run.
  - model: baseline_simple_cnn
  - logger: null
  - trainer: default
  - paths: default
  - extras: default
  - hydra: default

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: ???
